export class KTRLogger {
  // - only create if setting for logs is on (default off)
  // - create file for logs on .keep-the-rhyhtm folder
  // - if file already exists, continue
  // - expose function to add new log
  // - maybe a debounce/flush setup
}
